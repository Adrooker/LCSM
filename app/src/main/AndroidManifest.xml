<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lmcs">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LMCS">
        <activity android:name=".Useraccess2"></activity>
        <activity android:name=".Useraccess1" />
        <activity android:name=".renterpass" />
        <activity android:name=".otpentry" />
        <activity android:name=".forgotpass" />
        <activity
            android:name=".ui.login.ForgotPassword"
            android:label="@string/title_activity_forgot_password" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/mybutton" />

        <activity android:name=".Useracces" />
        <activity android:name=".signup" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>